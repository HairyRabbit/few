use std::env;
use std::fs;
use std::path::Path;

fn main() {
  // let dest_path = Path::new("src").join("counter_scss.rs");
  // fs::write(&dest_path,"pub static FOO: &'static str = \"btn\";").unwrap();
  println!("{}", "233");
}